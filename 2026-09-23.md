# 2026 年 9 月 23 日工作总结

## 今日完成

### 1. 建立项目 Agent 规则体系

- 创建中文 `AGENTS.md`，作为当前项目的主规则入口。
- 在 `specs/` 目录下拆分工程化细则，包括：
  - 工程架构与 Monorepo 规范
  - 前端、后端和共享包分层规范
  - TypeScript、Python、ESLint、Prettier 代码规范
  - 构建、质量门禁和验证规范
  - API、数据契约与安全规范
  - 前端、后端实现规范
  - 测试、Git 和团队协作规范
- 将 `Specs/` 修正为小写 `specs/`，统一目录引用。
- 明确规则是团队和 Agent 必须执行的工程约束，而不是参考建议。

### 2. 抽取并整理参考项目经验

- 分析 `1.basic/todo-app` 的 Monorepo、pnpm、Turborepo、前后端分层、共享包、Docker 和质量工具。
- 将通用工程经验抽离出来。
- 删除具体业务模块对通用规则的污染，避免把某个 Todo 示例误认为项目架构标准。

### 3. 按规则搭建 H5 Todo List

- 使用 React、Vite、TypeScript 构建移动端优先的 H5 Todo 应用。
- 按 `pages`、`components`、`hooks`、`services`、`types` 分层。
- 创建 `packages/core` 共享领域类型包和 `packages/react` 共享 UI 组件包。
- 完成任务新增、完成/取消完成、删除、筛选、清除已完成等功能。
- 完成移动端底部导航、安全区、触控区域和响应式布局。

### 4. 建立真实数据链路

- 移除 `localStorage` 数据方案。
- 新增 Node/TypeScript 后端 API 服务。
- 新增 Docker PostgreSQL 数据库。
- 使用 pgAdmin 提供数据库可视化查看能力。
- 当前数据链路为：

```text
H5 前端 -> services -> API -> PostgreSQL -> Docker
```

- 验证健康检查、创建、更新、删除和查询接口。
- 通过 `pnpm typecheck`、`pnpm lint`、`pnpm build` 完成质量验证。

## 这些工作的意义

### 从个人 Demo 变成团队工程

`AGENTS.md` 和 `specs/` 把个人经验转化为团队共同遵守的规则。后续成员或 AI 接手项目时，可以按照同一套架构、命名、分层和验证标准工作，减少理解偏差和重复沟通。

### 防止 AI 自行降低工程标准

明确禁止使用 `localStorage`、内存数组或前端 mock 作为正式业务数据源，并规定“没有后端就先创建后端”。这样 AI 不会为了快速完成页面而绕过真实工程链路。

### 支持多人并行协作

前端、后端、共享类型、共享 UI 和数据库职责分离后，团队可以并行开发。接口、类型和目录边界也更清晰，减少互相修改和代码冲突。

### 为后续扩展建立基础

当前 Todo 应用以后可以继续增加登录、权限、多端访问、管理后台、测试和部署，而不需要重新推翻数据层和项目结构。

### 建立可验证的完成标准

项目不再只是“页面能打开”，而是同时验证代码类型、Lint、构建、API、数据库和数据持久化。这样才能确认功能在团队环境中可复现、可维护、可交付。

## 今日形成的基础能力

```text
项目规则
  -> 工程架构
  -> 前后端分层
  -> 真实数据持久化
  -> 质量检查
  -> 团队与 AI 协作基础
```

## 后续方向

- 增加正式自动化测试。
- 增加用户认证和权限隔离。
- 将数据库初始化从自动建表升级为迁移机制。
- 增加生产环境配置和部署流程。
